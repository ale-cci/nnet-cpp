#ifndef __UTILS__
#define __UTILS__

#define ffor(x,END) for(int x = 0; x < END; ++x)

#define rand1() (((double) rand() * 2) / RAND_MAX -1)

//#define spt(x) (char*) &x
#define spt(x) reinterpret_cast<char *>(&x)

#endif // __UTILS__